import { FilterQuestionsPipe } from './filter-questions.pipe';

describe('FilterQuestionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuestionsPipe();
    expect(pipe).toBeTruthy();
  });
});
